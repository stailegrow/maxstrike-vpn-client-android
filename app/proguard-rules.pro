# R8/минификация включена в release-сборке (см. app/build.gradle.kts,
# isMinifyEnabled = true) — компилятор был отложен намеренно до этого
# захода ("включим ближе к публикации"), теперь самое время: без него
# release-код вообще не оптимизируется, а это одна из главных причин
# тормозов скролла в релизной сборке (developer.android.com/develop/ui
# /compose/performance — "Missing R8 Compiler Optimization").
#
# ВАЖНО (найдено после того, как первая версия этого файла сломала VPN —
# сайты переставали грузиться при подключении): нативный мост к
# Xray-core подключён строкой `implementation(files("libs/libXray.aar"))`
# в app/build.gradle.kts — это "плоская" локальная зависимость на файл, а
# не обычная library/maven-зависимость. Раньше здесь было написано, что
# proguard.txt внутри самого .aar (-keep class go.** / libXray.**)
# подключается автоматически — это предположение и оказалось причиной
# поломки: для `implementation(files(...))` AGP не всегда надёжно вытаскивает
# consumer-правила из .aar (в отличие от полноценной library-зависимости),
# так что в release-сборке эти правила вполне могли не примениться. Внутри
# .aar (проверено распаковкой classes.jar) есть
# libXray/LibXray$proxyDialerController.class — сгенерированный gomobile
# JNI-прокси, через который Go-ядро вызывает обратно в Java protectFd()/
# setDNS() (см. XrayCoreBridge.kt) — это ровно то, что не даёт собственным
# исходящим соединениям ядра Xray закольцеваться обратно в TUN. Если R8
# переименовал/убрал этот класс — JNI-вызов по имени метода перестаёт
# находить цель, защита сокета молча не срабатывает, и получается
# маршрутная петля: туннель "подключён", а трафик никуда не идёт — именно
# то, что сообщил пользователь после сборки с включённым R8.
#
# Чтобы больше не зависеть от того, подхватывает ли AGP consumer-правила
# из файловой AAR-зависимости, те же правила продублированы здесь явно —
# это не вредит, даже если AGP их и так подхватывает сам.
-keep class go.** { *; }
-keep class libXray.** { *; }

# MaxStrikeVpnService реализует libXray.DialerController напрямую (вызовы
# protectFd/... приходят из нативного кода через интерфейс) — как
# объявленный в манифесте Service он и так под стандартным keep-правилом
# AGP для компонентов, но держим явное правило, чтобы это не зависело от
# версии AGP/дефолтных правил.
-keep class com.stailegrow.maxstrike.vpn.MaxStrikeVpnService { *; }


# --- Защита от декомпиляции ---------------------------------------------------
# Все необфусцируемые классы переносятся в корневой пакет с короткими именами,
# разрешено менять модификаторы доступа ради более агрессивной оптимизации,
# имена исходных файлов в стектрейсах заменяются на "SourceFile".
-repackageclasses ''
-allowaccessmodification
-renamesourcefileattribute SourceFile

# Отладочные логи вырезаются из релизной сборки целиком.
-assumenosideeffects class android.util.Log {
    public static int v(...);
    public static int d(...);
}
